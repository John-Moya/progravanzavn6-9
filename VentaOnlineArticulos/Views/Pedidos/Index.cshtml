@model IEnumerable<VentaOnlineArticulos.Models.Pedidos>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.idArticulos)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.facha)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.idArticulos)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.facha)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.idPedido">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.idPedido">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.idPedido">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
