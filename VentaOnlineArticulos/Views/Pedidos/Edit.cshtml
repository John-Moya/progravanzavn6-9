@model VentaOnlineArticulos.Models.Pedidos

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Pedidos</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="idPedido" />
            <div class="form-group">
                <label asp-for="idArticulos" class="control-label"></label>
                <input asp-for="idArticulos" class="form-control" />
                <span asp-validation-for="idArticulos" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="facha" class="control-label"></label>
                <input asp-for="facha" class="form-control" />
                <span asp-validation-for="facha" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
